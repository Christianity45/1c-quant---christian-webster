# 1C Quant Project - Plastic Bottle Cap Regulation Analysis

This repository contains the data and code analyzing environmental cleanup volumes before, during, and after the EU tethered plastic bottle cap regulations.

## Required Python Libraries

To run the analysis notebook (`quant1c.ipynb`), you need to have the following packages installed. You can install them all via `pip`:

```bash
pip install pandas numpy scipy seaborn matplotlib
